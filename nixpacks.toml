[phases.setup]
nixPkgs = ["nodejs_22", "nodePackages.npm"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = [
  "npm run build",
  "npx prisma generate",
  "npx prisma migrate deploy"
]

[start]
cmd = "npm start"
